{
  "name": "reforge-ai-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "server": "node --env-file-if-exists=.env.local index.js",
    "start": "node --env-file-if-exists=.env.local index.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "express": "^5.2.1"
  }
}
